X scroll horizontally / verticallly and verify that flipping happens smoothly
X second layer
X vsync-interrupt driven updates
X 64x64 
X vload macro
X twinkle twinkle
o 16-way sprite
  o 16 colors
  o 16 rotations
  o 16 x 16
  o place in center
o 16-way scrolling movement
  o rotate ship
  o scroll appropriately
o fix bad tile bug in generator